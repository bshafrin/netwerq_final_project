<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All companies
    </h1>

    <a href="/companies/new" class="btn btn-block btn-outline-secondary">
      Add a new company
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Company name
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @companies.each do |company| %>
      <tr>
        <!-- Display company.id start -->
        <td>
          <%= company.id %>
        </td>
        <!-- Display company.id end -->

        <!-- Display company.company_name start -->
        <td>
          <%= company.company_name %>
        </td>
        <!-- Display company.company_name end -->

        <!-- Display company.created_at start -->
        <td>
          <%= time_ago_in_words(company.created_at) %> ago
        </td>
        <!-- Display company.created_at end -->

        <!-- Display company.updated_at start -->
        <td>
          <%= time_ago_in_words(company.updated_at) %> ago
        </td>
        <!-- Display company.updated_at end -->

        <td>
          <a href="/companies/<%= company.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @companies, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
